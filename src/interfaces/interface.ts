export interface Article {
    title: string,
    contents: string,
    imgSrc: string,
    date:string,
    link: string
};

export interface Markdown {
    title: string,
    contents: string
    date:string
}